import { Modal } from "react-bootstrap";

const ImageSelection = () => {
    return ( 
        <Modal.Body>
            images to select
        </Modal.Body>
     );
}
 
export default ImageSelection;